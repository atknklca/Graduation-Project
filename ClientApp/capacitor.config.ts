import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.angular.nny',
  appName: 'nny',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
